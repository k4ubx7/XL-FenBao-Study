# 贡献指南

感谢你帮助改进 XL-FenBao Study。

## 适合贡献的内容

- 可复现的解析、队列、命名、状态恢复和界面问题。
- 不绕过访问控制的站点兼容性修复。
- 隐私保护、错误脱敏、进程隔离和路径安全改进。
- 单元测试、离线端到端测试和构建文档。
- 更清晰的用户提示与无障碍体验。

## 不应提交

- Cookie、账号信息、Token、下载历史和真实浏览器配置。
- 未经授权的视频、字幕、封面或其他媒体。
- 无法确认来源或许可证的二进制文件。
- 默认绕过登录、付费、地区、验证码、DRM 或平台限制的功能。
- 只针对个人环境、却被描述为通用规则的改动。

## 本地检查

```powershell
npm ci
npm test
npm run typecheck
npm run build
```

涉及下载链路时，请增加对应单元测试或离线端到端测试。真实站点测试必须保持显式启用，不应成为普通 CI 的默认依赖。

## Pull Request

请在 PR 中说明：

1. 问题、预期行为和作用范围。
2. 方案选择及隐私、许可或兼容性影响。
3. 已运行的测试和可复现证据。
4. 是否引入新依赖、二进制或外部网络访问。

保持改动聚焦；不要把无关重构、格式化和功能修改混在同一个 PR 中。
